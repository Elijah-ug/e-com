// import React from "react";
// import { useParams } from "react-router-dom";
// import { useGetProductByIdQuery } from "../products/productsApi";

// export const NearProd = () => {
//   const prod = useParams()?.prod;
//   const parsedId = parseInt(prod);
//   const { data: productDetails, error, isLoading } = useGetProductByIdQuery(parsedId);

//   console.log("Product near me is==>", parsedId, typeof parsedId);

//   return <div>NearProd</div>;
// };
