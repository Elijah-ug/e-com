# TODOS
* signIn/login ==> JWT-based authentication Authentication 
  - bcrypt → for hashing passwords
  - jsonwebtoken → for creating & verifying tokens
  - Express middleware → to protect routes like /cart, /orders, etc.